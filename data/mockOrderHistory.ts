import { Order } from '../types';

// Mock data is now initialized in App.tsx from localStorage.
// This file can be kept for type reference or future static mock data.
export const mockOrderHistory: Order[] = [];
